name: Labeler
on:
  pull_request:
    types: [opened, synchronize]
jobs:
  label:
    permissions:
      contents: read
      pull-requests: write
    runs-on: arc-runner-set
    steps:
      - uses: actions/labeler@v4
